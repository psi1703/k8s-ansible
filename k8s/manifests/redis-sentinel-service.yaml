apiVersion: v1
kind: Service
metadata:
  name: otp-redis-sentinel
  namespace: otp-relay
  labels:
    app: otp-redis-sentinel
spec:
  type: ClusterIP
  selector:
    app: otp-redis-sentinel
  ports:
    - name: sentinel
      port: 26379
      targetPort: 26379
      protocol: TCP
