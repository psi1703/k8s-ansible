apiVersion: v1
kind: Service
metadata:
  name: otp-relay
  namespace: otp-relay
  labels:
    app: otp-relay
spec:
  type: ClusterIP
  selector:
    app: otp-relay
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
