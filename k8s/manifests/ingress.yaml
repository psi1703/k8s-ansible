apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: otp-relay
  namespace: otp-relay
spec:
  ingressClassName: traefik
  rules:
    - host: 
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: otp-relay
                port:
                  number: 80
  tls:
    - hosts:
        - 
      secretName: otp-relay-tls
