---
# Bundle-only release policy:
#
# Runtime Ansible deployment is disabled in this branch.
#
# This file is retained only for compatibility with legacy tooling that expects
# an Ansible collections requirements file to exist. The DEVtoPROD release-build
# path must not install K3s, configure hosts, apply manifests, run Helm, import
# images into a live cluster, or mutate production infrastructure.

collections: []
