---
# Ansible collection dependencies required by this repository.
#
# Install with:
#
#   ansible-galaxy collection install -r collections/requirements.yml
#
# The otp_relay_deploy role uses ansible.posix.synchronize, which is not
# guaranteed to be available from ansible-core alone on a fresh host.

collections:
  - name: ansible.posix
